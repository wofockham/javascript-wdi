%div#new_exercise

%div#chart

%div.row
  %div.large-4.columns
    %div.float
      = link_to('New', new_exercise_path, :remote => true, :class => 'button radius tiny alert')
    %div.float
      = button_tag('Chart', :class => 'button radius tiny', :id => 'show_chart')
    %div.float
      = select_tag 'activities', options_for_select(@activities.map { |a| [a, a]} )
    %div.clear
  %div.large-8.columns

%ol#exercises
  = render(:partial => 'exercises')