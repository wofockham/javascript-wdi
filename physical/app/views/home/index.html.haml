= link_to('New', new_user_path, :class => 'button radius tiny success')
= link_to('New', new_user_path, :remote => true, :class => 'button radius tiny alert')

%div#new_user

%ol#users
  = render(:partial => 'users/users')
